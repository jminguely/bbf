{% extends "base.twig" %}

{% block content %}
<div class="container">
  <div class="row">
    <div class="col-md-12 mt-7 mb-4">
      <a href="{{ posts|first.link }}" class="jumbotron jumbotron-fluid dark-gradient-overlay d-block bg-inverse text-white align-text-bottom">
        <div class="container jumbotron-content px-5">
          <div class="row">
            <div class="col-md-6">
              <time datetime="{{ posts|first.date('Y-m-d') }}">{{ posts|first.date('j F Y') }}</time>
              <h2 class="my-4">{{ posts|first.title }}</h2>
              <span class="btn btn-primary btn-lg" href="#" role="button">Lire l'article →</span>
            </div>
          </div>
        </div>
        <img class="cover-image" src="{{ posts|first.thumbnail.src("cover") }}">
      </a>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="row news-flow">
        {% for post in posts|slice(1) %}
          <div class="col-sm-6 col-md-4 mb-4 mt-3">
            {% include 'components/news/news-teaser.twig' %}
          </div>
        {% endfor %}
        {# <div class="col-md-12 mb-6 text-center">
          <a href="#" class="btn btn-primary">
            Plus de news
            <svg class="icon icon-right icon-arrow_right">
                <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#icon-arrow_right"></use>
            </svg>
          </a>
        </div> #}
      </div>
    </div>
  </div>
</div>
{% endblock %}
